# Operating system files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db
desktop.ini

# Editor / IDE state
.vscode/
.idea/
*.swp
*.swo
*~
.*.kate-swp
*.sublime-project
*.sublime-workspace

# Claude Code local state (per-machine, never committed)
# Ignore the contents, not the directory itself, so shared tooling can opt back in.
.claude/*
# ...except repo-level project commands, which are shared tooling
!.claude/commands/

# Python helpers (if added later)
__pycache__/
*.pyc
*.pyo
.pytest_cache/
.coverage
coverage/
.tox/
.venv/
venv/

# Node helpers (if added later)
node_modules/
npm-debug.log
yarn-debug.log
yarn-error.log
.npm/
.pnp.*

# Generic build / temp
dist/
build/
out/
*.tmp
*.bak
*.log
*.pdb

# ADR Guardian state (per-machine, never committed)
.adr-kit-state.json
docs/adr/.adr-kit-state.json
.adr-kit-state.json.lock
docs/adr/.adr-kit-state.json.lock
docs/adr/.adr-kit-readiness.json
.adr-kit-cache/

# Comprehensive review working output (session artefact, not source)
.full-review/
