{
  "schema_version": 1,
  "exceptions": [
    {
      "id": "claude-rich-workflow-source",
      "fixture": "clients/fixtures/claude-rich-workflow-source.json",
      "rationale": "Claude retains its richer native skill metadata while generated clients use the same compact procedure catalog.",
      "user_effect": "Claude receives richer discovery text; workflow outcomes and deterministic commands remain equal."
    },
    {
      "id": "copilot-pretool-context-limit",
      "fixture": "clients/fixtures/copilot-pretool-context-limit.json",
      "rationale": "Copilot cannot inject arbitrary model context at PreToolUse.",
      "user_effect": "Proactive deterministic checks and PostToolUse verification replace pre-edit context injection."
    },
    {
      "id": "codex-pr-guard-advisory-only",
      "fixture": "clients/fixtures/codex-pr-guard-advisory-only.json",
      "rationale": "The Codex hook adapter has no permission decision to return, so the pr-create guard cannot stop the tool call the way it does on Claude Code.",
      "user_effect": "The branch is still judged and the violation is shown, labelled as not enforced at this moment; the pre-commit hook and CI remain the blocking gates."
    },
    {
      "id": "copilot-lifecycle-event-limit",
      "fixture": "clients/fixtures/copilot-lifecycle-event-limit.json",
      "rationale": "Copilot CLI does not expose SubagentStart or PreCompact lifecycle hooks.",
      "user_effect": "Session and prompt context stay native, while generated workflow guidance requires selected ADR context to be carried forward without broadening."
    }
  ]
}
