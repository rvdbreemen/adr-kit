---
name: related
description: Show inbound and outbound references for one ADR and flag dangling links. Read-only.
license: MIT
---

<!-- Generated by scripts/build-client-adapters.py from clients/workflows.json; schema v1; client codex-cli. -->
# ADR relationships

Invoke explicitly with `$adr-kit:related` when needed.
Resolve `<plugin-root>` from this installed skill. Use only bundled,
local ADR Kit tools and follow this canonical workflow:

1. Run `adr-related <ADR-NNN> --adr-dir docs/adr --format json` and report typed inbound, outbound, dangling, and ambiguous links.

Do not contact another model or mutate user-owned instructions.
