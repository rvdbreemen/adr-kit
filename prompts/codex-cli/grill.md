<!-- Generated by scripts/build-client-adapters.py from clients/workflows.json; schema v1; client codex-cli. -->
# Grill an ADR decision

Use ADR Kit's `grill` skill in Codex CLI. This workflow may write files; show material changes before applying them.
Pass the remaining prompt as the workflow topic or target. Preserve the skill's confirmation and fail-open boundaries.
