[pytest]
pythonpath = .
markers =
    slow: marks tests as slow (wall-clock timing tests, skipped in fast CI with -m "not slow")
    tree_snapshot: needs the session-start copy of the working tree (tests/conftest.py)
